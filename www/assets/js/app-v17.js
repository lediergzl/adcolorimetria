/* ColorPro — catalog governance UI v1.7 */
import { DB } from './storage/db.js';
import { CatalogEngine } from './core/catalog-engine.js';
const $=s=>document.querySelector(s),esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function getCatalog(){const x=await DB.get('settings','catalogWorkspace');return Array.isArray(x?.catalog)?x.catalog:[]}
async function saveCatalog(catalog){await DB.setSetting('catalogWorkspace',{catalog,updatedAt:new Date().toISOString()})}
async function renderBrands(){const catalog=await getCatalog(),check=CatalogEngine.validateCatalog(catalog);$('#page-title').textContent='Marcas';$('#page-subtitle').textContent='Catálogo verificable';$('#view').innerHTML=`<div class="hero"><span class="pill">v1.7 · gobernanza de catálogo</span><h2>Base de productos</h2><p class="muted">Los datos técnicos comerciales solo se consideran utilizables cuando tienen fuente documental y están marcados como verificados.</p><div class="grid"><div class="card"><small>Registros</small><div class="metric">${catalog.length}</div></div><div class="card"><small>Verificados</small><div class="metric">${check.verifiedCount}</div></div><div class="card"><small>Errores</small><div class="metric">${check.errors.length}</div></div></div></div><div class="hero"><h3>Agregar producto</h3><form class="form" id="catalog17Form"><div class="field"><label>Marca</label><input name="brand" required></div><div class="field"><label>Línea</label><input name="line" required></div><div class="field"><label>Tono / código</label><input name="shade"></div><div class="field"><label>Categoría</label><select name="category"><option value="color">Color</option><option value="developer">Oxidante</option><option value="corrector">Corrector</option><option value="other">Otro</option></select></div><div class="field"><label>Proporción oxidante documentada</label><input name="developerRatio" type="number" min="0.01" step="0.01" placeholder="No asumir"></div><div class="field"><label>Fuente documental</label><input name="source" placeholder="Referencia exacta de ficha/fabricante" required></div><div class="field"><label>Notas</label><textarea name="notes" rows="3"></textarea></div><label><input type="checkbox" name="verified"> Marcar como verificado</label><button class="primary">Guardar registro</button></form></div><div class="hero"><div class="actions"><button class="secondary" id="export17">Exportar catálogo</button></div><div id="catalog17List">${catalog.map((p,i)=>`<div class="card"><b>${esc(p.brand)} · ${esc(p.line)}</b><p>${esc(p.shade||'Sin tono')} · ${esc(p.category)} · ${p.verified?'✓ verificado':'⚠ no verificado'}</p><p class="muted">Fuente: ${esc(p.source||'—')}</p><button class="secondary" data-del17="${i}">Eliminar</button></div>`).join('')||'<div class="empty">No hay productos cargados.</div>'}</div></div>`;
$('#catalog17Form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),p=CatalogEngine.normalizeProduct({brand:f.get('brand'),line:f.get('line'),shade:f.get('shade'),category:f.get('category'),developerRatio:f.get('developerRatio')||null,source:f.get('source'),notes:f.get('notes'),verified:f.get('verified')==='on'});const v=CatalogEngine.validateProduct(p);if(!v.valid){$('#catalog17List').innerHTML=`<div class="card">${v.errors.map(x=>`<div class="warning">⚠ ${esc(x)}</div>`).join('')}</div>`;return}await saveCatalog([...catalog,p]);renderBrands()};
document.querySelectorAll('[data-del17]');document.querySelector('#export17')?.addEventListener('click',async()=>{const blob=new Blob([CatalogEngine.exportCatalog(await getCatalog())],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='colorpro-catalog-v1.7.json';a.click();URL.revokeObjectURL(url)});
document.querySelectorAll('[data-del17]').forEach(b=>b.onclick=async()=>{const c=await getCatalog();c.splice(+b.dataset.del17,1);await saveCatalog(c);renderBrands()});
}
document.addEventListener('click',e=>{const b=e.target.closest('#nav button[data-view="brands"]');if(b){e.preventDefault();e.stopImmediatePropagation();renderBrands()}},true);window.ColorProCatalog17={renderBrands};
